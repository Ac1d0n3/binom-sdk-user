import { BnNoRolesDirective } from './bn-no-roles.directive';

describe('BnNoRolesDirective', () => {
  it('should create an instance', () => {
    const directive = new BnNoRolesDirective();
    expect(directive).toBeTruthy();
  });
});
