import { BnAuthDirective } from './bn-auth.directive';

describe('BnAuthDirective', () => {
  it('should create an instance', () => {
    const directive = new BnAuthDirective();
    expect(directive).toBeTruthy();
  });
});
