import { BnRolesDirective } from './bn-roles.directive';

describe('BnRolesDirective', () => {
  it('should create an instance', () => {
    const directive = new BnRolesDirective();
    expect(directive).toBeTruthy();
  });
});
